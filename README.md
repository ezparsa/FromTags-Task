# Questions about the `<form>` tag and attributes of `<input>` and `<button>`

## Questions

1. What is the role of the `<form>` tag in web pages and why is it essential for data submission? <hr>  
2. What is the difference between the `action` and `method` attributes in the `<form>` tag?  <hr>
3. When using `method="GET"` in a form, how are the data transmitted and what limitations exist? <hr> 
4. What is the purpose of the `type` attribute in `<input>`? Name five commonly used values.  <hr>
5. What is the difference between `<input type="submit">` and `<button type="submit">`?  <hr>
6. What is the role of the `value` attribute in `<input>` and when is it displayed?  <hr>
7. Why is the `name` attribute important in `<input>` and `<button>`, and how does it affect data processing?<hr>  
8. What is the difference between the `required` and `readonly` attributes, and what restrictions do they impose on the user?<hr>  
9. What does the `disabled` attribute do in `<input>` or `<button>`, and when is it typically used?  <hr>
10. What is the purpose of the `placeholder` attribute in `<input>` and how does it differ from `value`? <hr>
